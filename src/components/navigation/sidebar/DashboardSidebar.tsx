import React from "react";

const DashboardSidebar = ({ className }: { className?: string }) => {
  return (
    <aside className={`border-r-[1px] px-4 ${className}`}>
      DashboardSidebar
    </aside>
  );
};

export default DashboardSidebar;
