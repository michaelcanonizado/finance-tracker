import React from 'react';

const DashboardSidebar = ({ className }: { className?: string }) => {
	return <aside className={` ${className}`}>DashboardSidebar</aside>;
};

export default DashboardSidebar;
