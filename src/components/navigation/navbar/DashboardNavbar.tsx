import React from "react";

const DashboardNavbar = ({ className }: { className?: string }) => {
  return (
    <nav className={`h-16 border-b-[1px] px-4 ${className}`}>
      DashboardNavbar
    </nav>
  );
};

export default DashboardNavbar;
